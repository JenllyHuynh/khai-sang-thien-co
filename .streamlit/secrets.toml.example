# Sao chép file này thành `.streamlit/secrets.toml` (KHÔNG commit file đó - đã được .gitignore chặn sẵn) rồi điền API key thật của đạo hữu vào.
#
# Lấy key miễn phí tại: https://aistudio.google.com/apikey

GEMINI_API_KEY = "dan-api-key-that-vao-day"
