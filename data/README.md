# data/

Thư mục này hiện không chứa dữ liệu thô vì:

- **raw/**: Dữ liệu quay số thật không có sẵn công khai.
- **external/**: Không mượn dataset ngoài (vì lý do minh bạch, xem Trận Pháp 4)

Tất cả dữ liệu mô phỏng được sinh ra từ code trong `src/` và kết quả lưu tại `outputs/`.